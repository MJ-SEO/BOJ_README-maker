#ifndef TYPE
#define TYPE
#include <stdio.h>
#define MAX_LAN 256
#define CAPACIRY 256

typedef struct readme{
	int pnum;
	char p_title[MAX_LAN];
	char p_source[MAX_LAN];
	char elses[MAX_LAN];
} readme_t;



#endif
